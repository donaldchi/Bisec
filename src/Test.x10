import x10.util.*;
import x10.lang.*;

public class Test {
	public def sort( tt:HashMap[int, int]){

	}
	public static def main(args:Rail[String]) {

/*		var tt:HashMap[ int, int ];
		tt = new HashMap[int, int]();
		val r = new Random();
		for( i in (0n..20n)){
			var num:int = r.nextInt(100n);
			tt.put(i, num);
		}

		val tmap = new HashSet[int]();
		for ( i in (0n..20n)) {
			Console.OUT.println("key, value = " + i + ", " + tt.get(i));
			tmap.add(tt.get(i));
		}
		var it:Iterator[int] = tmap.iterator();
		while( it.hasNext()){
			Console.OUT.println(it.next());
		}

		var hashSet:ArrayList[NodeGroupPair];
		hashSet = new ArrayList[NodeGroupPair]();

		var ng:NodeGroupPair;
		ng = new NodeGroupPair(5n,10n);
		hashSet.add(ng);
		ng = new NodeGroupPair(1n, 1n);
		hashSet.add(ng);
		ng = new NodeGroupPair(2n, 104n);
		hashSet.add(ng);
		ng = new NodeGroupPair(3n, 20n);
		hashSet.add(ng);
		ng = new NodeGroupPair(4n, 15n);
		hashSet.add(ng);
		hashSet.sort();
		val itt = hashSet.iterator();
		while( itt.hasNext() ){
			var ngg:NodeGroupPair;
			ngg = itt.next();
			Console.OUT.println( "node, value: " + ngg.node + ", " + ngg.toGroupValue );
		}*/
	}
}
